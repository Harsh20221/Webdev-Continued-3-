<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>


  <form action="/recipe" method="POST" class="buttons
  ">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <%if (locals.recipe) {%> <!--- THIS if locals.recipe will make sure if the user has clicked on any recipe button then display the recipe name along with other information to the user -->

      <h2 id="recipeTitle">
        <%= recipe.name %>
      </h2>

      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= recipe.ingredients.protein.name %>, <%= recipe.ingredients.protein.preparation %>
        </li>
        <li>
          <%= recipe.ingredients.salsa.name %>

        </li>
        <% recipe.ingredients.toppings.forEach(topping=> { %> <!-- This lOOP WILL DISPLAY THE other toppings aling with quantity and name-->
          <li>
            <%= topping.quantity %> of <%= topping.name %>
          </li>
          <% }) %>
      </ul>

      <%} else {%> <!--THIS IS THE ELSE CONDITION , if there is no request sent by the user means no button clicked by the user then just dispplay the below heading Pick your favourite Taco ingredient to the user -->
        <h2>Pick your favourite taco ingredient 👆</h2> 
        <%}%>
  </div>
</body>

</html>